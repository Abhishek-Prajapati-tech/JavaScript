"use strict"; // treat all js code as newer version

// alert(3+3)   // we re using nodejs
console.log(3+3);
 let name = "abhishek"
 let age = 18
 let isLoggedIn = false
const i = 4646163
console.table([name , age, isLoggedIn , i]);

 // number => 2 to ki power 53
 // bigint
 // string => true/false
 // null

 // Numbers:
let length = 16;
let weight = 7.5;

// Strings:
let color = "Yellow";
let lastName = "Johnson";

// Booleans
let x = true;
let y = false;

// Object:
const person = {firstName:"John", lastName:"Doe"};

// Array object:
const cars = ["Saab", "Volvo", "BMW"];

// Date object:
const date = new Date("2022-03-25");

let a = "16" + "Volvo";
let b = 16 + "Volvo";

console.table([name , age, isLoggedIn , i , x ,y ,person , cars, date,color, lastName ,length,weight , a , b]);